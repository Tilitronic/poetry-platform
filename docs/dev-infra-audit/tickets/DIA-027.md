# DIA-027 — pnpm audit 13 vulnerabilities (10 high)

---

- **ID**: DIA-027
- **Title**: pnpm audit 13 vulns (10 high)
- **Area**: deps
- **Severity**: Major
- **Status**: FIXED
- **Discovered**: fix-lane (wave 2) · 2026-08-02

---

## Description

`pnpm audit` reported **13 vulnerabilities, 10 high** in the JavaScript dependency
tree — a material supply-chain risk for the JS gates and the dev loop. Major because
high-severity advisories were unresolved and nothing documented the residual risk.

## Verification

1. `pnpm audit` → 13 vulns, 10 high (pre-fix).

## Fix

`3d3a180` remediated the tree: bumped `quasar` to `^2.22.0` plus targeted
overrides → **0 critical / 0 high**. One residual low remains and is documented:
`esbuild` `GHSA-g7r4-m6w7-qqqr` via `@quasar/app-vite` 2.x — no released fix exists,
and it only affects the Windows dev-server path, so it is accepted and tracked.

## Re-verify

`pnpm audit` → **0 critical / 0 high**; the single residual low (`esbuild`
GHSA-g7r4-m6w7-qqqr via @quasar/app-vite 2.x, Windows dev-server only) is
documented in the fix commit with the "no released fix" rationale.
