# DIA-009 — test-interview-enforcement.sh orphaned

---

- **ID**: DIA-009
- **Title**: test-interview-enforcement.sh orphaned — not in Makefile despite CHANGELOG claim
- **Area**: scripts
- **Severity**: Minor
- **Status**: OPEN
- **Discovered**: inventory N2 (CHANGELOG vs Makefile) · 2026-08-02

---

## Description

`scripts/test-interview-enforcement.sh` implements 5 checks (OMO presets deny
`openspec-propose` to orchestrator, banned phrases absent, interview-first present,
`/tdd-cycle` via `@openspec-plan`, boss fast-path gate). The OpenCode CHANGELOG
claims it is registered in Makefile test targets, but it is **NOT** in the Makefile —
it is manual-only. Either register it under a `make` gate (e.g. `test-config`) and
verify it passes, or update the CHANGELOG claim.

## Verification

1. `grep -n test-interview-enforcement Makefile` → expect no match.
2. `grep -n test-interview-enforcement .opencode/CHANGELOG.md` → expect claim.

## Fix

> To be filled at fix time.

## Re-verify

> To be filled at re-verify time.
