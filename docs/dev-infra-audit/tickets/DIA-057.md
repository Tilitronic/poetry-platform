# DIA-057 — Knowledge-workflow violation — conspect not created on research request (2026-08-06)

<!-- Owner-reported process gap (m0023) in the §11 knowledge workflow
     (research → conspect → analysis). Backlog-only ticket: documents the
     violation, root cause, and fix recommendations. NO code/config change is
     made by this ticket; fix is process/orchestrator discipline + documentation,
     awaiting owner scheduling. -->

---

id: DIA-057
title: "Knowledge-workflow violation — conspect not created on research request (2026-08-06)"
area: docs
severity: Low
status: VERIFIED
blocked_by: ["restart+smoke"]
discovered: 2026-08-06
source: owner-reported
date: 2026-08-06
created: 2026-08-06
updated: 2026-08-08

# --- Session Attribution (v2 schema, optional — GRANDFATHERED for DIA-001..049) ---

session_id: "ses_0286983baffe92mVnkUBjxARa4"
lane_id: ""
agent: "coder"
model: ""
parent_session_id: ""
attempts: 0
lease_expires_at: ""
files_touched: ["docs/dev-infra-audit/tickets/DIA-057.md", "docs/dev-infra-audit/tickets/README.md"]
artifacts: []
evidence: ["knowledge/ana007-session-log-silencing/ana007-session-log-silencing-report.md", "knowledge/res002-silent-session-logging/res002-silent-session-logging-conspect.md"]

---

## Description

**Summary:** research request m0001 ("silent session-logging") dispatched
@ai-specialist (ai--1) + @researcher (res-1), which returned findings
in-conversation only. NO `knowledge/res*/sources/` + conspect +
`shelf.conspects` entry was produced at research time — violating §11
knowledge-workflow order (research → conspect → analysis). The analysis
(ana007) was authored BEFORE the conspect existed; the owner flagged the gap
(m0023); the conspect was then built postfactum (res002, 10 archived sources,
6 synthesis sections, MLA-cited, 2026-08-06) and ana007 was re-grounded
against it.

**Root cause:** routing gap — research lanes persisted findings only in
conversation; the orchestrator did not verify knowledge artifacts (sources/ +
conspect + shelf entry) at research completion. No @conspecter step was
dispatched at research time, so the conspect requirement was never enforced
before analysis started.

**Secondary finding:** the analyzer runtime has NO general file-write tool (no
`write`; `ast_grep_replace` is unreliable on markdown bold-colon nodes) →
analyzer doc persistence must fall back to coder lanes (documented in the
ana-3 stop-report). This makes the analyzer structurally unable to author
knowledge artifacts itself, reinforcing the need for the @conspecter step to
run before analysis.

**Impact:** low severity (no production risk, no gates broken) but a
recurring-process risk — without the conspect gate, analyses can be authored
on un-archived, conversation-only research and cannot be re-grounded or
cited, undermining the knowledge base's traceability contract.

**Linked artifacts:** m0001 (campaign), ana007 (re-grounded report), res002
(conspect), DIA-051 (adjacent JSONL-leak backlog ticket — same
session-logging area).

## Verification

1. Confirm the order violation from the artifact timeline: research session
   ids `ses_02892a8acffe48eyG6JzXDpwrg` (ai--1) and
   `ses_0288eaaf0ffeRaNsDfvAAi7brp` (res-1) predate the conspect file
   `knowledge/res002-silent-session-logging/res002-silent-session-logging-conspect.md`,
   while the initial ana007 report predates the conspect (revision note in the
   re-grounded report documents this).
2. Confirm no conspect/shelf entry existed at research time: the conspect's
   `created` is 2026-08-06 postfactum; shelf entry `res002` was added at the
   same time.
3. Post-fix (when scheduled): dispatch a research request and verify, at
   research completion, that `knowledge/res*/sources/` + conspect +
   `shelf.conspects` entry all exist BEFORE any analysis is dispatched.

## Fix

**Applied 2026-08-07 (§10 cycle, full fix A+D+E):**
(A) delegation-observer.ts researcher-lane-scoped PERSISTENCE_RECOMMENDED
trigger (L709-762, writes `.opencode/session/persistence-pending.json`,
pure-additive); (D) orchestrator_append.md Research Persistence Gate hard-gate
(L49-71); (E) NEW `agents/analyzer.md` + `agents/conspecter.md`. CHANGELOG
entry added. Review: ai-auditor APPROVE-WITH-FINDINGS (3) — all accepted.
Verification: `make test-config` exit 0 + `tsc --noEmit` project-flags exit 0.

**Fix recommendations (owner-directed context, 2026-08-06):**

1. **Research dispatches must include the @conspecter step** (or an explicit
   conspect requirement) BEFORE analysis — the §11 chain
   (research → conspect → analysis) becomes an enforced gate, not a
   convention.
2. **Orchestrator verifies knowledge artifacts** (sources/ + conspect + shelf
   entry) exist at research completion before dispatching analysis — a
   checklist step in the research dispatch protocol.
3. **Document the analyzer write-tool limitation + coder fallback** in the
   analyzer agent definition or lessons (analyzer has no `write` tool;
   `ast_grep_replace` unreliable on markdown bold-colon nodes; doc
   persistence falls back to coder lanes per ana-3 stop-report).

**§10 routing note:** fixes 1-2 are process/orchestrator discipline
(documentation + prompt/agent-definition changes); if they touch
`.opencode/` agent definitions, route through §10 (AI Devtools Modernization
Workflow) when implemented. Fix 3 is documentation-only.

**Out of scope:** no code/config change in this ticket; backlog item awaiting
owner scheduling.

## Re-verify

**VERIFIED 2026-08-08 (campaign close).** Status IMPLEMENTED → VERIFIED.

- **Pipeline outcome verified via res003:** the res003 campaign
  (`knowledge/res003-telemetry-reentrancy-guards/`) completed the full
  knowledge-workflow chain — 39/39 archived sources
  (`res003-...-sources/.source-urls.txt`), conspect
  (`res003-telemetry-reentrancy-guards-conspect.md`, 90 lines, MLA-cited),
  `shelf.conspects` entry added 2026-08-08, independently verified.
  Same-session pipeline run (manual dispatch via the §11 chain) satisfies
  Verification item 3: sources/ + conspect + shelf entry all exist before
  analysis.
- **Mechanical trigger still not operational (separate gap):** the res-2 smoke
  returned `PERSISTENCE_RECOMMENDED: true` but
  `.opencode/session/persistence-pending.json` was NOT written.
- **Root cause (HIGH confidence):** delegation-observer.ts L736 state-check
  regex `/state:\s*completed/i` expects colon/header form `state: completed`;
  actual OpenCode task() output uses XML-attribute form `state="completed"` →
  AND condition always FALSE.
- **Fix tracking:** see DIA-068 (VERIFIED) — form-tolerant regex / fix of the
  persistence trigger. Re-verified via DIA-068 restart-verify.
- **Mechanical trigger NOW FIRES (2026-08-08, DIA-068 restart-verify):** the
  DIA-068 regex fix was verified in a fresh session — positive smoke (res-1,
  ses_01e8307d3ffeAI39rpfiNCq8re) wrote
  `.opencode/session/persistence-pending.json`; negative control (ai--1,
  ses_01e7f794bffeMw7WJ4IGjkQAvG — NON-researcher lane whose output contained
  the literal `PERSISTENCE_RECOMMENDED: true`) did NOT re-create the file.
  Mechanical trigger gap CLOSED — DIA-057 → FULLY VERIFIED.
