# DIA-060 — Orchestrator read scope missing tickets directory

<!-- Discovered 2026-08-07: the orchestrator's path-scoped read permissions do not include
     docs/dev-infra-audit/tickets/**/*, so the orchestrator cannot directly read open tickets
     at session boot. It must delegate to a subagent (e.g., @code-navigator) to discover them.
     This is unnecessary delegation cost and violates the principle that the orchestrator
     should have direct visibility into the project's ticket system. -->

---

id: DIA-060
title: "Orchestrator read scope missing tickets directory"
area: opencode-config
severity: Blocker
status: OPEN
blocked_by: []
discovered: 2026-08-07
source: fix-lane
date: 2026-08-07
created: 2026-08-07
updated: 2026-08-07

# --- Session Attribution (v2 schema, optional) ---

session_id: ""
lane_id: ""
agent: "code-executor"
model: ""
parent_session_id: ""
attempts: 0
lease_expires_at: ""
files_touched: ["docs/dev-infra-audit/tickets/DIA-060.md"]
artifacts: []
evidence: []

---

## Description

**Summary:** the orchestrator's path-scoped read permissions (defined in the system
prompt / permission rules) only allow reads from:
- `.opencode/session/*`
- `docs/dev-infra-audit/NEXT-RUN.md`
- `AGENTS.md`
- `.opencode/practice-protected.md`

The ticket ledger at `docs/dev-infra-audit/tickets/` is NOT in the allowlist. This
means at session boot, the orchestrator cannot directly read open tickets — it MUST
delegate to a subagent (e.g., @code-navigator) to discover them. This is an
unnecessary delegation cost and violates the principle that the orchestrator should
have direct visibility into the project's ticket system.

**Why this is a Blocker:** the orchestrator is the primary session driver. It
decides what to work on from the ticket ledger. Forcing it to delegate ticket
discovery to a subagent at every boot adds latency, burns tokens, and introduces
a brittle dependency — if @code-navigator fails or is unavailable, the orchestrator
has zero visibility into what needs to be done. The orchestrator should own its
primary input channel directly.

**Desired state:** the orchestrator's read permissions should include
`docs/dev-infra-audit/tickets/**/*` so it can read ticket files directly at boot
and during sessions.

**Related:** DIA-059 (the §10 gate plugin, which would enforce the workflow, is
also not yet activated — needs a restart).

## Verification

1. **Confirm current gap:** from a fresh session, the orchestrator attempts to
   `read` any file under `docs/dev-infra-audit/tickets/` → expect PERMISSION DENIED
   or the file operation is refused (the tickets directory is outside the allowlist).

2. **Confirm delegation workaround:** orchestrator delegates to @code-navigator to
   list and read ticket files → succeeds. This proves the data is reachable but only
   via an unnecessary subagent hop.

3. **Post-fix (after permission rules updated):**
   - Orchestrator calls `read` on `docs/dev-infra-audit/tickets/DIA-060.md` →
     expect ALLOWED (file contents returned)
   - Orchestrator calls `glob` with pattern `docs/dev-infra-audit/tickets/DIA-*.md` →
     expect ALLOWED (matching files returned)
   - Orchestrator calls `read` on `docs/dev-infra-audit/tickets/README.md` →
     expect ALLOWED
   - Verify the orchestrator does NOT gain write access to the tickets directory
     (read scope only)

4. **Build validation:**
   - `make test-config` → exit 0
   - No JSONC/YAML syntax errors in modified config files

## Fix

> To be filled at fix time.

**Fix approach:**

Route through §10 AI Devtools Modernization Workflow (AGENTS.md §10, AGENTS.md §2.5):

1. **@ai-specialist** researches the correct permission pattern and best practices
   for orchestrator read scoping:
   - Identify where orchestrator read permissions are defined (system prompt,
     opencode.jsonc permission block, or oh-my-opencode-slim.jsonc)
   - Research whether `docs/dev-infra-audit/tickets/**/*` is the correct glob
     pattern for the permission system in use
   - Research whether there are upstream best-practice rules about orchestrator
     read scope (e.g., `opencode-best-practices.md`)

2. **Update the orchestrator's read permission rules** to include
   `docs/dev-infra-audit/tickets/**/*`. The exact location depends on @ai-specialist's
   findings — candidates are:
   - `.opencode/opencode.jsonc` → orchestrator tool permission block
   - `.opencode/oh-my-opencode-slim.jsonc` → orchestrator agent preset config
   - The orchestrator's system prompt (if read paths are embedded there)

3. **Validate** that the orchestrator can read `docs/dev-infra-audit/tickets/` after
   the change (see Verification §3).

4. **Phase 5 validation** (§10 workflow): restart OpenCode, run smoke test
   confirming orchestrator can read ticket files, verify no regression in
   `make test-config`.

5. **Phase 6 independent review:** dispatch @ai-auditor to verify the permission
   change against best practices.

**§10 routing note (MANDATORY):** this fix touches `.opencode/` config → it MUST
route through §10 (AI Devtools Modernization Workflow): @ai-specialist gate →
design → @coder → @ai-specialist independent review → restart + smoke.

## Re-verify

> To be filled at re-verify time. Acceptance: orchestrator can `read` and `glob`
> `docs/dev-infra-audit/tickets/**/*` directly without delegation; `make test-config`
> exit 0; @ai-auditor Phase 6 review passes.
