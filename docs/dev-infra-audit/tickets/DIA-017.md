# DIA-017 — ai-assist-sources.yaml path consistency

---

- **ID**: DIA-017
- **Title**: ai-assist-sources.yaml — verify path consistency; close as verified if moot
- **Area**: opencode-config
- **Severity**: Minor
- **Status**: FIXED
- **Discovered**: inventory (audit-plan M5) · 2026-08-02

---

## Description

Audit-plan item M5 flagged a path concern for `ai-assist-sources.yaml`. Inventory
reconciliation finds the file **EXISTS** at
`.opencode/oh-my-opencode-slim/knowledge/`, making the original path concern moot.
Verify the path used by config/references matches the actual file location, record
the evidence, and close as verified (or document the residual mismatch if any).

## Verification

1. `ls .opencode/oh-my-opencode-slim/knowledge/ai-assist-sources.yaml` → expect exists.
2. Grep config for `ai-assist-sources` and confirm referenced path resolves.
3. Record evidence → close ticket as verified.

## Fix

`edaa21d` pointed the `ai-specialist` prompt's knowledge-source reference at the
project-relative `ai-assist-sources.yaml` file, matching the actual location.

## Re-verify

`ai-specialist` config references resolve to the project-relative
`.opencode/oh-my-opencode-slim/knowledge/ai-assist-sources.yaml`, which exists;
path consistent across config and prompt.
