# DIA-043 — Husky hooks not CI-enforced (no CI exists)

---

id: DIA-043
title: "Husky hooks not CI-enforced (no CI exists)"
area: git-hooks
severity: Minor
status: CLOSED
blocked_by: []
discovered:
source: inventory
date: 2026-08-04
created: 2026-08-04
updated: 2026-08-04

---

## Description

Inventory finding: `package.json` declares `prepare: "husky"` and a full
lint-staged config; `scripts/verify-pre-commit.sh` and
`scripts/verify-pre-push.sh` exist and are bats-tested (4 + 4 cases). The hook
scripts **are** git-tracked — `git ls-files .husky/` lists `.husky/pre-commit`
and `.husky/pre-push` (per DIA-008 resolution, commit `6ba7200`); only the husky
v9 scaffolding (`git check-ignore .husky/_`) is gitignored and regenerated by
`pnpm prepare`.

**Corrected premise (2026-08-04):** the original draft premise "no committed
.husky/" was stale — the hooks ARE committed. What remains true, and is the real
gap: the hooks are enforced **only** by git events, developer-local. **No CI
exists** (no `.github/`), and **no Makefile gate runs the hooks**, so a developer
who commits with `--no-verify`, or a fresh clone whose hooks were not regenerated
by `pnpm prepare`, bypasses the entire autofix/verify chain with no gate firing.
Re-scoped 2026-08-04 from "no committed .husky/" to "not CI-enforced (no CI
exists)" and downgraded Medium → Minor (hygiene/enforcement gap — no gate
impact). The commands in Verification settle the committed-state question; the
enforceable-gap part stands regardless.

## Verification

1. `git ls-files .husky/` → record which hook files are tracked (2026-08-04: `pre-commit`, `pre-push`).
2. `git check-ignore .husky/_` → husky v9 scaffolding is gitignored/regenerated by `pnpm prepare`; confirm `core.hooksPath` wiring (`git config core.hooksPath`).
3. `grep -rn "verify-pre-commit\|verify-pre-push\|lint-staged" Makefile .github/ 2>/dev/null` → confirm no gate or CI enforces the hooks.
4. Confirm `pnpm prepare` regenerates hooks from the tracked files on a fresh clone (`pnpm install` in a clean checkout).
5. Record results in Re-verify.

## Fix

> **NEEDS SPEC (§2.4) — flagged 2026-08-04 (code-executor, dev-infra lane),
> NOT implemented.** The ticket's Fix section was empty at fix time (no
> owner-approved action), and both candidate fixes are design decisions, not
> mechanical edits:
>
> - **Option A — add minimal CI** (`.github/workflows/` GitHub Actions: e.g.
>   `pnpm install` — regenerates husky hooks via `prepare` — + `pnpm verify` +
>   `make test-shell`): exceeds ~20 lines, picks a CI provider/trigger/scope,
>   and per §2.4 needs an openspec-plan spec. Viable: the repo remote IS
>   GitHub (`origin https://github.com/Tilitronic/poetry-platform.git`), no
>   `.github/` exists today.
> - **Option B — document as intentional**: hooks stay developer-local;
>   enforcement is dev-side via the tracked `.husky/` hooks, with
>   `git commit --no-verify` as the explicit escape hatch; no CI on this
>   repo.
>
> Per the lane ground rules the sub-task was STOPPED at the unresolved design
> choice and reported as **needs spec** — dispatch `@openspec-plan` if Option A
> is chosen (spec first, then implementation).

> **RESOLVED 2026-08-04 — owner disposition (Option B: Document + close):**
> intentionally not CI-enforced. No CI infrastructure exists on this repo
> (GitHub-hosted, but no `.github/` workflows); husky/lint-staged run
> developer-local via the tracked `.husky/` hooks wired through
> `core.hooksPath` (`.husky/_`) and regenerated by `pnpm prepare`. Verification
> evidence recorded in Re-verify: `pnpm prepare` exit 0, `core.hooksPath`
> intact. **No CI was added**; this ticket is **closed by owner disposition**
> (disposition-based close, not code-verified — status `CLOSED`, matching
> DIA-049). Option A (add CI) remains spec-able later via `@openspec-plan` if
> the owner changes direction.

## Re-verify

Verification evidence 2026-08-04 (code-executor, read-only — recorded for the
owner's disposition):

1. `git ls-files .husky/` → `.husky/pre-commit`, `.husky/pre-push` (hooks ARE
   tracked; confirms the corrected premise).
2. `git check-ignore .husky/_` → directory itself not reported ignored, but
   `.husky/_` is **untracked** (`git ls-files .husky/_` empty) and its contents
   are ignored via the husky-generated nested `.husky/_/.gitignore` containing
   `*` (`git check-ignore -v .husky/_/husky.sh` → `.husky/_/.gitignore:1:*`);
   `git config core.hooksPath` → `.husky/_` (husky v9 wiring intact).
3. `grep -rn "verify-pre-commit\|verify-pre-push\|lint-staged" Makefile .github/`
   → no matches; `.github/` does not exist → confirmed: **no gate or CI
   enforces the hooks** (the enforceable-gap part of the finding stands).
4. `pnpm prepare` (husky) executed 2026-08-04 on this checkout — exit 0
   (`> poetry-platform-monorepo@ prepare … > husky`); after the run
   `git config core.hooksPath` → `.husky/_` (unchanged) and
   `git ls-files .husky/_` → 0 (scaffolding regenerated but stays untracked).
   This confirms the regenerate-on-install path; a clean-clone check
   (`pnpm install` in a fresh checkout) exercises the same `prepare` script.
5. Recorded above. Status stays OPEN (needs spec).
