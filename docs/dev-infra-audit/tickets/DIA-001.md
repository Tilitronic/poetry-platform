# DIA-001 — references.shelf.path points to directory `.opencode` instead of `memory-shelf.yaml`

---

- **ID**: DIA-001
- **Title**: references.shelf.path points to directory `.opencode` (opencode.jsonc:227)
- **Area**: opencode-config
- **Severity**: Minor
- **Status**: FIXED
- **Discovered**: inventory (audit-plan m3, CONFIRMED unfixed) · 2026-08-02

---

## Description

In `.opencode/opencode.jsonc` line 227, the `references.shelf` entry points at the
directory `.opencode` instead of the shelf index file `.opencode/memory-shelf.yaml`.
The memory-shelf.yaml is the central index of all RAG knowledge bases, research
conspects, specs, and analyses — pointing at the directory makes the shelf reference
ambiguous. Inventory reconciliation confirms: "references.shelf.path → still
directory .opencode at opencode.jsonc:227, CONFIRMED unfixed".

## Verification

1. Read `.opencode/opencode.jsonc` line 227 and confirm the shelf path value.
2. Run `make test-config` — passes regardless (validator does not check shelf shape).
3. Manually confirm the file `.opencode/memory-shelf.yaml` exists.

## Fix

`8adf250` changed `references.shelf.path` in `.opencode/opencode.jsonc` from the
directory `.opencode` to the shelf index file `.opencode/memory-shelf.yaml`.

## Re-verify

`references.shelf.path` in `.opencode/opencode.jsonc:227` now reads
`.opencode/memory-shelf.yaml` (a file, not a directory); `.opencode/memory-shelf.yaml`
exists; `make test-config` passes.
