# DIA-005 — Ghost agents code-navigator / researcher

---

- **ID**: DIA-005
- **Title**: Ghost agents code-navigator/researcher — verify usage; document or remove
- **Area**: opencode-config
- **Severity**: Minor
- **Status**: CLOSED
- **Discovered**: inventory (audit-plan M4) · 2026-08-02

---

## Description

`code-navigator` and `researcher` are defined as agents in `.opencode/opencode.jsonc`
but are rarely (or never) dispatched in practice — "ghost agents". Note the
reconciliation finding: M4 is _partially moot_ because `code-navigator` is now
actively used. Verify actual usage for both, then either document them as first-class
agents or remove the unused definition. `researcher` remains a strong candidate for
removal.

## Verification

1. Grep session logs / command definitions for dispatches of `code-navigator` and
   `researcher`.
2. Check `.opencode/opencode.jsonc` agent blocks for both.

## Fix

**Moot** — both `code-navigator` and `researcher` are actively used in the current
workflow (recon and external research lanes); neither is a ghost agent anymore.

## Re-verify

Closed as moot: both agents are dispatched in active workflows; definitions remain
as first-class agents.
