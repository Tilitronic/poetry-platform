# DIA-023 — tools/opencode-docker/TODO.md stale line refs and F13 version

---

- **ID**: DIA-023
- **Title**: tools/opencode-docker/TODO.md stale line refs + F13 version
- **Area**: docker
- **Severity**: Minor
- **Status**: FIXED
- **Discovered**: fix-lane (wave 2) · 2026-08-02

---

## Description

`tools/opencode-docker/TODO.md` (security todo for the read-only opencode-docker
image) carried stale line references and an outdated version default:

1. F9 gitconfig references no longer matched `bin/opencode-docker:113-118`.
2. F10 still listed the bare `/app` root mount as open, but the root mount was
   already removed (only `/app/...` subdir mounts remain).
3. F13 recorded `OPENCODE_VERSION=unknown` as the default, but the Dockerfile now
   pins a concrete version.

Stale refs make the security ledger unreliable as the authoritative source.

## Verification

1. Read `tools/opencode-docker/TODO.md` and diff the F9/F10 line refs against
   `bin/opencode-docker`.
2. `grep -n OPENCODE_VERSION tools/opencode-docker/Dockerfile` → expect concrete
   version, not `unknown`.

## Fix

`3642906` refreshed `TODO.md`: F9 line refs updated to the current
`bin/opencode-docker:113-118` gitconfig `:ro,Z` handling, F10 marked resolved (no
bare `/app` root mount remains), F13 updated to the pinned version, and the audit
date refreshed to 2026-08-02.

## Re-verify

`tools/opencode-docker/TODO.md` audit date reads 2026-08-02; F9 refs match
`bin/opencode-docker`; F10 marked `[x]` resolved; F13 documents the pinned
`OPENCODE_VERSION` (1.18.4) in `Dockerfile:6` — no stale `unknown` default.
