# DIA-026 — format drift in 7 files

---

- **ID**: DIA-026
- **Title**: format drift 7 files
- **Area**: js-tooling
- **Severity**: Minor
- **Status**: FIXED
- **Discovered**: fix-lane (wave 2) · 2026-08-02

---

## Description

Seven files drifted from the repository's prettier formatting (JS/TS/markdown/JSON
under the `pnpm format` glob). `pnpm verify:format` (`prettier --check`) failed on
those files, keeping the format gate red — a clean-cycle blocker at Minor severity.
The drift accumulated as earlier fix-lane commits were authored without running
prettier.

## Verification

1. `pnpm verify:format` → prettier --check reports the 7 non-conforming files.

## Fix

`7db1aa8` applied `prettier --write` to the 7 drifted files (format-only commit, no
behavior change).

## Re-verify

`pnpm verify:format` → exit 0; `prettier --check` clean across the repo.
