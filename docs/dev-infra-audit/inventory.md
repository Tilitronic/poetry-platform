# Dev-Infrastructure Inventory — poetry-platform

Repo root: /home/qualt/Projects/poetry-platform · Git tracked, hooksPath .husky/\_ · No .github/ directory exists — no CI config anywhere.

## 1. Root Toolchain

- Workspace scripts (package.json): dev/build/lint/typecheck/clean = turbo run <task>; stack = bash scripts/dev-stack.sh; format/format:check = prettier; test = turbo test; verify:format→format:check; verify:js→lint+typecheck; verify:js-tests→test; verify:python→scripts/verify-python.sh; verify = all four; prepare = husky.
- pnpm-workspace.yaml: globs apps/_ + packages/_; onlyBuiltDependencies: @parcel/watcher, esbuild.
- turbo.json tasks: compile:lezer, generate:phonetic-atlas, codegen, build (dependsOn compile:lezer+^build), test (dependsOn build), dev (cache:false, persistent), lint, typecheck, format (cache:false). Note: codegen/generate:phonetic-atlas inputs reference paths existing only in packages/phonetics-core.
- .npmrc: shamefully-hoist=true (audit-plan m1 — antipattern, unaddressed).
- .gitignore: node_modules, dist, .turbo, .env*, jsconfig.json, knowledge/*/sources/, knowledge/context7-docs/, scripts/**tests**/vendor/, tools/opencode-docker/{workspace,homebase,secrets,.worktrees,worktrees,opencode.json,.opencode,.cache}, secrets/\* except secrets/README.md.
- .gitattributes: _ text=auto eol=lf; _.lock text eol=lf.
- .prettierignore: deps/build/Python/generated + .opencode/, tools/, secrets/, knowledge/, scripts/**tests**/vendor/, jsconfig.json.

## 2. Linters / Formatters / Static Analysis

- eslint.base.config.js: JS recommended + Vue flat/essential + TS rules (consistent-type-imports, no-unused-vars w/ ^\_ ignore); ignores apps/api-server, packages/analytics-pipeline, generated/.
- eslint.config.js (root): [...baseConfig]. apps/author-studio/eslint.config.js: base + @quasar/app-vite/eslint + vueTsConfigs.recommendedTypeChecked + PWA globals.
- .prettierrc.json: singleQuote, printWidth 100.
- lint-staged (package.json): js/ts/vue → eslint --fix + prettier --write; css/scss/html/md/json/yaml → prettier --write; apps/api-server/\*_/_.py → scripts/lint-python-files.sh; sh/bats → bash -n.
- pyrightconfig.json: include api-server + analytics-pipeline, python 3.13, basic mode.
- Ruff: apps/api-server/pyproject.toml [tool.ruff] line-length 100, E+F+I. packages/analytics-pipeline/pyproject.toml has NO ruff config and no dev deps.
- scripts/lint-python-files.sh: lint-staged bridge (cd api-server, uv-venv, ruff check --fix + ruff format).
- No mypy config anywhere.

## 3. TypeScript Configs

- Root tsconfig.json (ESNext/Preserve/Bundler, strict, noEmit) + per-package extends with paths @poetry/data-contracts → schemas/contract.json etc. author-studio extends generated .quasar/tsconfig.json (produced by quasar prepare postinstall). Typecheck: tsc --noEmit (packages) / vue-tsc --noEmit (author-studio).

## 4. Git Hooks (husky v9)

- core.hooksPath = .husky/\_ (husky v9 scaffolding, gitignored, regenerated by pnpm prepare).
- .husky/pre-commit → bash scripts/verify-pre-commit.sh (npx lint-staged --allow-empty; FAILS exit 1 when dev container down; --allow-empty for no-staged-files).
- .husky/pre-push → bash scripts/verify-pre-push.sh (pnpm verify:format → verify:js → verify:js-tests → verify:python; warns+passes when container down).
- Test coverage: scripts/**tests**/verify-pre-commit.bats (4), verify-pre-push.bats (4). Gate: make test-shell.
- PROBLEM: git status shows .husky/pre-push staged but .husky/pre-commit untracked — pre-commit hook risks vanishing on fresh clone.

## 5. Makefile Targets

stack (scripts/dev-stack.sh host bootstrap) · build (docker compose build dev) · up (docker compose up -d) · shell (docker compose exec dev bash) · opencode (docker compose exec -it dev opencode) · dev (exec -it pnpm dev) · install · db-psql · logs · down · clean (down -v) · gen-jsconfig (scripts/gen-jsconfig.sh > jsconfig.json) · test-shell (bats-wrapper.sh) · test-infra (gen-jsconfig test-shell → test-docker-smoke.sh → up --build → test-python → down) · test-python (uv venv + uv pip install -e ".[dev]" + pytest in container) · test-config (prereqs test-interview + test-skills, then docker compose config --quiet → validate-opencode-config.sh → validate-agent-names.sh → validate-output-contracts.sh → validate-reviewer-sections.sh → validate-handoff.sh → test-ticket-gate.sh → audit-agent-tool-coverage.sh x2 (both configs) → node scripts/**tests**/batch-d-infra.test.mjs) · context7-docs (node scripts/context7-docs.mjs; dry-run without key; NOT in test-infra — quota).

- PROBLEM: RESOLVED 2026-08-14 — scripts/test-interview-enforcement.sh IS wired via make test-interview (Makefile:159-160), itself a prereq of test-config; the stale CHANGELOG-vs-Makefile gap was closed by DIA-009.

## 6. scripts/

dev-stack.sh (host bootstrap: .env from example → docker-info gate → compose up --build → conditional pnpm install → pnpm dev; bats 5) · dev-secrets-profile.sh (/etc/profile.d/secrets.sh re-exporting 5 whitelisted /run/secrets files into interactive shells — fixes `docker compose exec` losing entrypoint exports; copied by Dockerfile.dev:188; covered by dev-entrypoint.bats 10) · gen-jsconfig.sh (jsconfig.json from pnpm-workspace + packages; bats 5) · lint-python-files.sh (ruff bridge; bats 6) · test-docker-smoke.sh (heavy: build+start, healthchecks, runtimes, playwright, openspec 1.7.0, make, secrets mounts, LS binaries, pg_isready, Xvfb probes, tini PID1, author-studio :9000 HTTP 200 if node_modules present; self is the test) · test-interview-enforcement.sh (5 checks: OMO presets deny openspec-propose to orchestrator, banned phrases absent, interview-first present, /tdd-cycle via @openspec-plan, boss fast-path gate; wired: make test-interview → test-config prereq) · verify-pre-commit.sh (autofix gate; fails when container down; bats 4) · verify-pre-push.sh (full gate; warns+passes when container down; bats 4) · verify-python.sh (uv venv → ruff check . → ruff format --check . → pytest; bats 4) · context7-docs.mjs (no deps; Context7 pipeline, dedupe, retry policy, dry-run/mock/real modes, API base https://context7.com; bats 16 + fixtures).

## 7. Docker

- docker-compose.yml: dev service (Dockerfile.dev, ports 9000/8000/3000, volumes .:/workspace:Z + pnpm_store/dev_state/dev_cache, env DATABASE_URL + 5 API keys, 5 file secrets → /run/secrets, depends_on postgres healthy, tty, restart unless-stopped) + postgres service (postgres:16-alpine, pg_isready healthcheck 5s/10 retries, pgdata volume).
- Dockerfile.dev: debian:13-slim @ SHA256; pinned ARGs (node 24.18.0, opencode 1.18.4, pnpm 10.33.0, bun 1.3.14, snip 0.22.0, openspec 1.7.0, uv 0.11.29, mise v2026.8.0, rust 1.83.0, tini 0.19.0, TS-LS 5.3.0, pyright 1.1.411); SHA256-verified installs; Xvfb/xclip/rg/jq/make/dbus; Playwright+crawl4ai PLAYWRIGHT_BROWSERS_PATH=/opt/ms-playwright; non-root dev UID 1000; healthcheck opencode --version; ENTRYPOINT tini -- /usr/local/bin/dev-entrypoint.sh; CMD bash.
- dev-entrypoint.sh: load 5 whitelisted secrets → env; Xvfb :99 -ac -noreset; exec "$@". Whitelist must stay in sync with dev-secrets-profile.sh + compose secrets (smoke-tested).
- .dockerignore: .git, node_modules, dist, .turbo, .venv, .env*, secrets/, docs/knowledge/*.md.
- tools/opencode-docker/: Dockerfile (3-stage, read-only rootfs, --cap-drop at runtime, openspec 1.6.0 -- DIFFERS from Dockerfile.dev 1.7.0), Makefile (podman, --read-only --tmpfs /tmp:exec --cap-drop=ALL --no-new-privileges --shm-size=1g --memory=4g --cpus=4), config/opencode.json (envsitter-guard@0.0.4, telemetry@0.1.19, context7+gh_grep MCP), bin/opencode-docker wrapper (flock instance lock), bootstrap.py (larger ALLOWED_SECRETS incl. google/aws), TODO.md (F1-F16, F7/F8/F9/F10/F11 open), AGENTS.md.
- .devcontainer/devcontainer.json: dockerComposeFile ../docker-compose.yml, service dev, workspaceFolder /workspace, shutdownAction stopCompose, 7 extensions, postCreate pnpm install && gen-jsconfig.sh, forwardPorts 9000/8000/3000, remoteUser dev.
- Docs: docs/docker-dev.md (CONTAINER-SETUP.md removed 2026-08-14 as superseded, DIA-176).

## 8. OpenCode Configuration

- .opencode/opencode.jsonc: default_agent orchestrator; compaction auto/prune; permission baseline (edit allow, sudo/git push/remote denied, destructive ask); lsp: true; agents (orchestrator edit/write/bash deny -- mechanical delegation; architector deny; analyzer; reviewer; coder unrestricted w/ M2 intent comment; code-navigator; researcher; designer; observer; explorer; memory-manager; council deny; ai-specialist subagent qwen3.7-plus edit deny bash curl/wget); provider ollama (qwen2.5-coder:14b, qwen3-coder:30b, gemma3:12b); references.shelf; plugins (envsitter-guard@0.0.4, file:///workspace/.opencode/plugins/delegation-observer.ts, file:///workspace/.opencode/plugins/needs-input-observer.ts -- project array; telemetry@0.1.19 and OMO@2.2.13 are NOT in the project array -- they load from the global ~/.config/opencode/opencode.jsonc pin); MCP context7 (Bearer, oauth:false, timeout 15000) + gh_grep; commands tdd-cycle/test-package/arch-check/code-ownership.
- .opencode/oh-my-opencode-slim.jsonc (39KB): presets opencode-go (qwen3.7-max), cebula (deepseek-v4-flash chain), free (big-pickle); orchestrator skills ["*","!openspec-propose"]; disabled_agents [oracle,fixer,explorer,librarian]; vendored fork at .opencode/oh-my-opencode-slim/ (REFERENCE-ONLY; npm 2.2.13 live).
- .opencode/tui.json: {} (plugin key dropped P1).
- .opencode/practice-protected.md: 4 zones (spec authoring, TDD edge cases, architector decisions, review disposition) + 3-tier permission classification + ownership tracking.
- .opencode/memory-shelf.yaml: res001 conspect, ana002/ana004 analyses, 1 ADR, spec context7-docs-pipeline (implemented).
- .opencode/agents/memory-manager.md: writes adr.md/lessons.md/repo.md/failures.md.
- .opencode/commands/: opsx-propose/new/continue/update/sync/apply/archive/explore + rag + telemetry-inspect/telemetry-report.
- .opencode/skills/ (15): book-rag, console-charting, debugging-workflow, frontend-design, git-diff, mermaid-diagramming, openspec-apply-change/archive-change/explore/propose/sync-specs/update-change, playwright-browser, teaching, writing-skills.
- .opencode/scripts/: validate-opencode-config.sh (make test-config), query_rag.py, query_web.py, test_query_rag.py, test_query_web.py.
- .opencode/CHANGELOG.md: 2026-08-02 context7 MCP fix, P1–P9/N1–N5 config audit, orchestrator-first default, cebula cleanup, interview-first enforcement.

## 9. Test Infrastructure

- Vitest: packages/editor-engine/vitest.config.ts, packages/phonetics-core/vitest.config.ts (include src/\*_/_.test.ts, node env) — only these 2 run real JS tests; root has no vitest config.
- Pytest api-server: apps/api-server/pyproject.toml dev extras pytest>=8, pytest-asyncio, ruff; asyncio_mode=auto; gate make test-python / pnpm verify:python. Pytest analytics: packages/analytics-pipeline/pyproject.toml asyncio_mode auto only — NO dev deps, NO tests dir, NOT covered by verify-python.sh.
- Bats wrapper: scripts/**tests**/bats-wrapper.sh — bash -n over 10 shell scripts + node --check context7-docs.mjs; system bats or vendored bats-core v1.11.0 (gitignored vendor/); gate make test-shell. Suites: context7-docs(16), dev-entrypoint(10), lint-python-files(6), dev-stack(5), gen-jsconfig(5), verify-pre-commit(4), verify-pre-push(4), verify-python(4) + test-helper.bash (mock_docker, unshare isolation) + context7-mock fixtures.
- Playwright: skill only; container Playwright probed by smoke test; no committed E2E test files.

## 10. Docs / Process

- docs/docker-dev.md, docs/onboarding.md, docs/workflow-loop.md (6-phase loop), docs/dev-infra-audit-plan.md (C/M/m status).
- AGENTS.md (root + apps/author-studio + tools/opencode-docker): feature chain architector → openspec-plan → coder → reviewer; design authority order; practice-protected.
- architecture.md (52KB), .sdd/README.md (3 planned module docs, none authored).
- openspec/config.yaml + openspec/changes/: context7-docs-pipeline (implemented), dev-infra-language-servers, dev-infra-stack-hardening (implemented, 7 tasks), pre-commit-autofix (proposed); NO openspec/specs/ dir.
- knowledge/: res001, ana002, ana004, context7-docs.
- PROBLEMS: onboarding.md references nonexistent prompts/ dir (lines 303/323/333); onboarding agent/skill tables stale (test-architect, feature-interviewer, 6 agents); onboarding claims "api-server — not yet created" (contradicts existing apps/api-server).

## 11. Secrets / Env Handling

- secrets/: README.md tracked + 5 key files, all gitignored (git ls-files = README only). Permissions: anthropic/exa/github/openai keys are 0-byte empty placeholders; context7_api_key 44B real; README advises chmod 600 but files are default perms.
- Compose mounts secrets → /run/secrets/<name> read-only in dev container. Whitelist (5): anthropic/openai/context7/github/exa, mirrored by dev-secrets-profile.sh + compose (M2 sync enforced by smoke test).
- .env (gitignored) has 6 keys (POSTGRES\_\*, ports) — MISSING CONTEXT7_API_KEY that .env.example declares (pending user step; compose default :- makes it non-fatal).
- Smoke probe skips mount checks when host file empty.

## 12. CI — .github/ does NOT exist. All gates local: make test-infra / make test-config / pnpm verify + husky hooks.

## 13. .vscode

- settings.json: formatOnSave, source.fixAll explicit, prettier default, eslint.validate js/ts/vue/html/markdown, LF+trim, ruler 100, ruff python formatter, rust-analyzer clippy, typescript.tsdk node_modules/typescript/lib.
- extensions.json: 12 recs (eslint, prettier, remote-containers, remote-wsl, markdown-preview-enhanced, editorconfig, python, pylance, debugpy, ruff, rust-analyzer, vue.volar).

## Reconciliation of docs/dev-infra-audit-plan.md

DONE (verified): C1 (compose+Dockerfile stack), C2 (OMO slim in Docker), C3 (configs merged), C4 (agent naming), C5 (plugin pinning), C6 (ai-specialist rights), M2 (coder unrestricted), M9 (preset cleanup), m6, m7, m8.
IN-PROGRESS (commit pending): M1 (devcontainer.json), M7 (postgres defined).
OPEN: M4 (ghost agents — now partially moot: code-navigator actively used), M5 (ai-assist-sources.yaml — file EXISTS at .opencode/oh-my-opencode-slim/knowledge/, path concern moot), M6 (api-server production Dockerfile), M8 (split ai-specialist), m1 (shamefully-hoist), m2 (ports — substantively done), m3 (references.shelf.path → still directory .opencode at opencode.jsonc:227, CONFIRMED unfixed), m5 (skills-lock only cli-review), m9 (duplicate agents in system prompt).

## New findings (not in old plan)

N1: .husky/pre-commit untracked (would vanish on clone). N2: test-interview-enforcement.sh orphaned (CHANGELOG claims Makefile registration). N3: onboarding.md prompts/ broken ref + stale content. N4: openspec version skew (tools/opencode-docker 1.6.0 vs Dockerfile.dev 1.7.0 — 1.6.0 is the false-green version the smoke test guards against). N5: .env missing CONTEXT7_API_KEY. N6: analytics-pipeline pytest config without dev deps/tests/ruff; verify-python covers api-server only. N7: empty secret placeholder files (4/5) violate README non-empty rule (by design; smoke test skips empty).
