// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { AtlasMetadata, AtlasMetadataT } from './atlas-metadata.js';
import { PhonemeEntry, PhonemeEntryT } from './phoneme-entry.js';


export class PhoneticAtlas implements flatbuffers.IUnpackableObject<PhoneticAtlasT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):PhoneticAtlas {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsPhoneticAtlas(bb:flatbuffers.ByteBuffer, obj?:PhoneticAtlas):PhoneticAtlas {
  return (obj || new PhoneticAtlas()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsPhoneticAtlas(bb:flatbuffers.ByteBuffer, obj?:PhoneticAtlas):PhoneticAtlas {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new PhoneticAtlas()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('PHAT');
}

metadata(obj?:AtlasMetadata):AtlasMetadata|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new AtlasMetadata()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

phonemes(index: number, obj?:PhonemeEntry):PhonemeEntry|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new PhonemeEntry()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

phonemesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startPhoneticAtlas(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addMetadata(builder:flatbuffers.Builder, metadataOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, metadataOffset, 0);
}

static addPhonemes(builder:flatbuffers.Builder, phonemesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, phonemesOffset, 0);
}

static createPhonemesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startPhonemesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endPhoneticAtlas(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // metadata
  builder.requiredField(offset, 6) // phonemes
  return offset;
}

static finishPhoneticAtlasBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, 'PHAT');
}

static finishSizePrefixedPhoneticAtlasBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, 'PHAT', true);
}

static createPhoneticAtlas(builder:flatbuffers.Builder, metadataOffset:flatbuffers.Offset, phonemesOffset:flatbuffers.Offset):flatbuffers.Offset {
  PhoneticAtlas.startPhoneticAtlas(builder);
  PhoneticAtlas.addMetadata(builder, metadataOffset);
  PhoneticAtlas.addPhonemes(builder, phonemesOffset);
  return PhoneticAtlas.endPhoneticAtlas(builder);
}

unpack(): PhoneticAtlasT {
  return new PhoneticAtlasT(
    (this.metadata() !== null ? this.metadata()!.unpack() : null),
    this.bb!.createObjList<PhonemeEntry, PhonemeEntryT>(this.phonemes.bind(this), this.phonemesLength())
  );
}


unpackTo(_o: PhoneticAtlasT): void {
  _o.metadata = (this.metadata() !== null ? this.metadata()!.unpack() : null);
  _o.phonemes = this.bb!.createObjList<PhonemeEntry, PhonemeEntryT>(this.phonemes.bind(this), this.phonemesLength());
}
}

export class PhoneticAtlasT implements flatbuffers.IGeneratedObject {
constructor(
  public metadata: AtlasMetadataT|null = null,
  public phonemes: (PhonemeEntryT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const metadata = (this.metadata !== null ? this.metadata!.pack(builder) : 0);
  const phonemes = PhoneticAtlas.createPhonemesVector(builder, builder.createObjectOffsetList(this.phonemes));

  return PhoneticAtlas.createPhoneticAtlas(builder,
    metadata,
    phonemes
  );
}
}
