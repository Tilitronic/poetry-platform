ROOT CAUSE (DIA-260829-kxqu, CORRECTED): `log_decision` was absent because the local plugin `.opencode/plugins/delegation-observer.ts` FAILS TO LOAD under Bun with `failed to load plugin ... error="Plugin export is not a function"` (seen 8x in ~/.local/share/opencode/log/opencode.log on 2026-08-28, persisting). OpenCode is a Bun-built ELF; Bun's TS loader rejects a construct in this 222KB/4969-line file (the smaller needs-input-observer.ts loads fine; Node's experimental-strip-types accepts both). NOT a missing dependency (node_modules + @opencode-ai/plugin@1.18.10 present), NOT a permission issue (log_decision:allow at opencode.jsonc:230), NOT agent gating (none in code). FIX: bisect under Bun, make the module Bun-parseable (minimal structural edit, behavior unchanged), add scripts/validate-plugin-loads.sh Bun-load guard, restart OpenCode to confirm log_decision appears. Validation: `npx bun -e "import m from './.opencode/plugins/delegation-observer.ts'; typeof m.default"` must print 'function'.
