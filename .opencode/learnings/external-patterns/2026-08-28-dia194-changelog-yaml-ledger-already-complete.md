DIA-194 CHANGELOG->YAML ledger conversion is ALREADY COMPLETE via DIA-196 + DIA-205 (both CLOSED). YAML (.opencode/CHANGELOG.yaml) is the machine-first source of truth; CHANGELOG.md is the derived render target (scripts/changelog-render L1-9). Tooling wired: scripts/changelog-add (one-call append+validate+render), scripts/validate-changelog.sh in make test-config, JSON schema gate. AGENTS.md L99 + orchestrator_append.md L380-416 document YAML-first queries. The handoff note 'conversion NOT started' is STALE. Recommendation: status quo, no further conversion. EBDV: A status-quo (recommended), B legacy-ref cosmetic cleanup (not recommended), C retire MD view (rejected during DIA-196).
