Ponytail is ACTIVE at 'full' via project plugin array (.opencode/opencode.jsonc:717); DIA-183 is CLOSED (headroom spiked, NOT-RECOMMENDED). OMO jsonc schema has NO ponytail config knobs — ponytail is a plugin (hooks-based), not an OMO skill; adding it to skills arrays is a no-op. 19 dangling 'simplify' skill refs exist across all presets (cosmetic debt, already silently ignored). Variant B (remove them) = trivial zero-risk cleanup, no behavioral delta, needs restart. Variants C (PONYTAIL_DEFAULT_MODE=ultra) and D (PONYTAIL_SUBAGENT_MATCHER) are env-var/ponytail-config-file changes, OUT of 2.5 scope.
