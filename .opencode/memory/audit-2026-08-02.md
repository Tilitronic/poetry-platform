Dev-infra audit summary (branch: further-dev-infrastructure-development)

Date: 2026-08-02

This file records irrecoverable operational knowledge and human context discovered during the dev-infra audit that are not recoverable from git history, diffs, tests, or code.

- Audit result: 1 clean testing cycle achieved — all 12 automated gates pass, zero open Blocker/Critical tickets. Local ticket ledger: docs/dev-infra-audit/tickets/ (DIA-001..035). 27 fix commits applied on top of baseline 6ba7200.

- Key decisions (not fully reconstructible from diffs): methodology formalised as inventory → per-feature vertical testing → fix-per-ticket → full re-verification until clean-cycle definition met (all gates pass + zero Blocker/Critical). Ledger retained under docs/dev-infra-audit/tickets/ for human context.

- Notable infra facts (operational, not code): .npmrc shamefully-hoist=true was removed (commit c28a57d) and proved safe (lockfile unchanged; pnpm+turbo host+container gates pass). Volta v2.0.2 installs could not be sha256-verified (no official checksum) — documented blocker.

- Python packaging: uv.lock committed for both Python packages (e9781ff); make audit-python established and uses `uv export --no-dev` + `uvx pip-audit@2.10.1 --disable-pip -r` because pip-audit cannot read uv.lock and host lacks ensurepip.

- Docker & secrets: Dockerfile.dev now sha256-verifies node + rustup installs (5174931); compose removed API-key env passthrough and moved to secrets-only consumption (fa272f3).

- Testing patterns and failures: bats login-shell PATH shadowing (test 50) fixed by isolating HOME in hermetic tests (d96b8e4); lint-staged change for .bats vs .sh (b7d4c05); pnpm exit 126 fixed by installing pnpm via Volta + frozen-lockfile install; three hashtag parsing bugs fixed in .opencode/scripts/query_rag.py (6eba89d).

- Parallel work lessons: parallel fix lanes with disjoint write scopes worked but required strict per-commit scoping and sequencing verification after writers to avoid ledger races (final close d179764 resolved the race). Fixture syncs can break other lanes (feacb1c).

- Files/places of interest (navigational pointers): docs/dev-infra-audit/ (tickets/ + run-logs/), scripts/__tests__/vendor/ (vendorised bats-core), .opencode/opencode.jsonc and tools/opencode-docker/config/opencode.json (Context7 MCP registration), scripts/dev-stack.sh, Makefile targets test-shell/test-infra/test-config.

Stored here because these are human operational lessons, audit summary, and navigational context not fully reconstructible from repository commits alone.
