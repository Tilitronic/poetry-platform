Run pytest for the analytics-pipeline Python package: cd packages/analytics-pipeline && python -m pytest -xvs $ARGUMENTS

Show any failures with the full traceback. If there are import errors, check pyproject.toml dependencies and installed packages.
